"use strict";

class GuestbookEntry {
    constructor(title, content) {
        this.title = title;
        this.content = content;
    }
}

module.exports = GuestbookEntry; 
// export default GuestbookEntry; das ist neuere Version von dieser Schreibweise: Export